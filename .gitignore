# Python
__pycache__/
*.py[cod]
.venv/
.ruff_cache/
.pytest_cache/

# Streamlit local secrets (NEVER commit)
.streamlit/secrets.toml
app/.streamlit/secrets.toml

# Snowflake / credentials
token/
*.p8
*-token-secret.txt

# OS
.DS_Store

# direnv (local env overrides — copy from .envrc.example)
.envrc

# Terraform (state files may contain secrets; use remote backend)
terraform/terraform.tfvars
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl

# CoCo / IDE artifacts
.snowflake/
.coverage
app/output/
